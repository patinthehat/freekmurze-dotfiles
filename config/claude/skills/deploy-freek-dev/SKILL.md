---
name: deploy-freek-dev
description: Deploy the freek.dev website. Use when the user says 'deploy freek.dev', 'deploy the site', 'ship it', 'push to production', or wants to deploy freek.dev to production.
---

# Deploy freek.dev

To deploy freek.dev, run these commands sequentially using Bash:

```
commit;push;deploy-code
```
